"""Rendering and result assembly for :mod:`wreath.series`.

Split from the public module for the usual reason: the declaration surface is
small and worth reading, while the SQL it renders and the envelope it assembles
are neither. Nothing here is imported by name from outside ``wreath.series``.
"""

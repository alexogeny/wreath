# `wreath` CLI

The `wreath` command runs applications (`wreath run`, `wreath dev`), generates
typed clients (`wreath typegen`), and queries a running server's telemetry
(`wreath inspect`, a client of the [Inspector](inspector.md) socket). Use
`wreath --help` for the full flag list; run options map onto
[`ServerConfig`](server.md).

::: wreath.cli

# `wreath.objects`

Object/blob storage: a pluggable `ObjectStore` protocol, pathlib-like `ObjectPath`, local + S3 backends (zero-dep SigV4, no boto3), and streaming Zip64 archives.

::: wreath.objects

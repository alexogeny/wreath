# `wreath.aws_lambda`

Dependency-free ASGI adaptation for API Gateway payload v1/v2 and Lambda
Function URLs. One adapter instance owns one event loop and one ASGI lifespan
across warm invocations.

::: wreath.aws_lambda

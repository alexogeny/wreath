# `wreath.middleware`

The middleware protocol plus built-ins: CORS, security headers, compression, rate limiting, request IDs, server timing, proxy headers, CSRF, sessions, and trusted hosts.

::: wreath.middleware

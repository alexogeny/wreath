# `wreath.app`

Construct and run an application, register routes and lifecycle, and dispatch requests. The `Wreath` class is the entry point; route decorators live on `Wreath` and `Router`.

::: wreath.app
